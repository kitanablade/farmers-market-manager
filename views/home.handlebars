<aside>
    <div class="container text-center">
    <div class="row">
        <div class="col-12 sidebar">
        <a href="/" id="home">Home</a>
        </div>
        <div class="col-12 sidebar">
        <a href="/event" id="events">Event List</a> 
        </div>
        <div class="col-12 sidebar">
        <a href="/vendor" id="vendors"> Vendor List</a>
        </div>
    </div>
</aside>
<section>
    {{#each events}}
    <div class= "card">
        <h3><a href="/event/{{id}}">{{eventName}}</a></h3>
        <h4>{{description}}</h4>
    </div>
    {{/each}}
</section>